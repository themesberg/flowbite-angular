export type StandardThemeConfiguration = {
  base: {
    light: string;
    dark: string;
  };
  hover?: {
    light: string;
    dark: string;
  };
  focus?: {
    light: string;
    dark: string;
  };
  disabled?: {
    light: string;
    dark: string;
  };
};
