import { AddonDirective } from './addon.directive';

describe('AddonDirective', () => {
  it('should create an instance', () => {
    const directive = new AddonDirective();
    expect(directive).toBeTruthy();
  });
});
